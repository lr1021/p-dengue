m1: multi_normal_prec
m2: car_normal (is not built-in, GPT hallucination)
m3: sparse_car implementation
m4: sparse_car vectorised sampling
m5: simple beta + log_pop poisson model
m6: sparse_car tau=1
m7: beta + log_pop negative binomial dispersion
m8: negative binomial + independent gaussian noise (good convergence but high noise variance)
m9: neg bin + random + car (no convergence)
m10: beta + log_pop negative binomial + zero inflation (bernoulli mask)
m11: some efficiency adjustments on m10
m12: some efficiency adjustments on m11
m13: m12 + gaussian noise (for each location and time step) (good convergence, posterior noise samples show some spatial correlation, have quite high posterior variance)
m14: m13 + soft sum to zero constraint on eps as they had negative mean, not sure why beta_0 was not capturing that
m15: m12 + surveillance data (also no 0 bound on beta_0)
m16: m15 + urban
m17: m12 without lower bound 0 on beta_0
m18: m16 + urban density
m19: m7 corrected beta_0 prior (beta_0 + log_pop NB)
m20: beta_0 + log_pop + log_surveillance NB
m21: beta_0 + log_pop + log_surveillance + urban_pop/area NB
m22: beta_0 + log_pop + log_surveillance + urban_pop/area + urban_density NB (fairly small density effect on standardised urban_denxity_X_urban_density)
m23: m21 + 1 variable spline (0 lag)
m24: m23 spline template

Welc0me@2026
ewa.nus.edu.sampling/mnt/data